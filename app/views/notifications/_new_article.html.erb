<% if notification.target %>
    <div class="media-heading">
        <p class="notifications-p"><i class="fa fa-edit text-secondary"></i>| <%= link_to notification.actor.username, main_app.profile_path(notification.actor.username) %> published a new article titled <%= link_to notification&.target&.title, main_app.article_path(notification.target) %></p>
    </div>
<% else %>
    <p class="text-danger"> <i class="fa fa-trash"></i>* Action has been deleted. *</p>
<% end %>